apiVersion: v1
kind: Service

metadata:
  name: food-delivery-service

  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

spec:

  selector:
    app: food-delivery

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
